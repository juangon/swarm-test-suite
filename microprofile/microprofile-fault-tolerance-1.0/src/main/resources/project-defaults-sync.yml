swarm:
  hystrix:
    command:
      default:
        fallback:
          isolation:
            semaphore:
              maxConcurrentRequests: 20
        execution:
           isolation:
              thread:
                 timeoutInMilliseconds: 30000
